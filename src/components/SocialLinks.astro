---
import { SOCIAL_LINKS } from "../consts";
import IconLabel from "./IconLabel.astro";
---

<ul class="mt-3 flex flex-col gap-1.5">
  {
    SOCIAL_LINKS.map((link) => (
      <li>
        <IconLabel icon={link.icon} href={link.url} label={link.label} />
      </li>
    ))
  }
</ul>
